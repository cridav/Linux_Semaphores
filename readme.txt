Main code is in BUFFER.c
BUFFER.h is the header
bufchat.c is the testing code, it executes the compiled files generated from bufprob.c, bufproa.c and bufrea.c

the non-mentioned files are implemented with only queues, no buffers
